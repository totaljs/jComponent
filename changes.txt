======= 4.0.0 (BETA)

- added: a small time delay for `data-component-class` attribute
- added: `component.html()` changes the content when a new value is different than element content

- updated: a default component supports validation for all controlls when they contain `required` attribute
- updated: `data-component-template` downloads the template only one time (download cache for 5 minutes)

- improved: `component.html()`, `component.append()`